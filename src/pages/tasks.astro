---
import IndexLayout from '@/layouts/IndexLayout.astro'
import Kanban from '@/components/widgets/Kanban.astro'
import { themeConfig } from '@/config'
---

<IndexLayout title={`Tasks · ${themeConfig.site.title}`} description={themeConfig.site.description} addTopMargin={true}>
  <p class="description">kanban board with bigger goals and tasks</p>

  <Kanban />
</IndexLayout>

<style>
  h1 {
    font-size: var(--font-size-m);
    font-weight: var(--font-weight-bold);
    margin-bottom: 2rem;
  }

  .description {
    color: var(--text-secondary);
    font-size: var(--font-size-s);
    margin-bottom: 2rem;
  }
</style>
