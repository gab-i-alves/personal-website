---
import IndexLayout from '@/layouts/IndexLayout.astro'
import Currently from '@/components/widgets/Currently.astro'
import { themeConfig } from '@/config'
---

<IndexLayout title={`Now · ${themeConfig.site.title}`} description={themeConfig.site.description} addTopMargin={true}>

  <p class="description">
    what I'm currently doing, updated whenever I feel like it.
  </p>
  <Currently />
</IndexLayout>

<style>
  h1 {
    font-size: var(--font-size-m);
    font-weight: var(--font-weight-bold);
    margin-bottom: 2rem;
  }

  .description {
    color: var(--text-secondary);
    font-size: var(--font-size-s);
    margin-bottom: 2rem;
  }
</style>
