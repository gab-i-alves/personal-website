---
import IndexLayout from '@/layouts/IndexLayout.astro'
import Bookmarks from '@/components/widgets/Bookmarks.astro'
import { themeConfig } from '@/config'
---

<IndexLayout title={`Bookmarks · ${themeConfig.site.title}`} description={themeConfig.site.description} addTopMargin={true}>

  <p class="description">curated list with things I find interesting</p>
  <Bookmarks />
</IndexLayout>

<style>
  h1 {
    font-size: var(--font-size-m);
    font-weight: var(--font-weight-bold);
    margin-bottom: 2rem;
  }

  .description {
    color: var(--text-secondary);
    font-size: var(--font-size-s);
    margin-bottom: 2rem;
  }
</style>
